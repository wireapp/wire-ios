sources:
- ${PACKAGE_ROOT_DIR}/Sources/WireReusableUIComponents
templates:
- ${TARGET_DIR}/Sourcery/AutoMockable.stencil
output:
  ${DERIVED_SOURCES_DIR}
args:
  autoMockableImports: ["WireReusableUIComponents"]
