name: Playground

on: workflow_dispatch

permissions: 
  checks: write

jobs:
  playground:
    uses: ./.github/workflows/app_release.yml
    with:
      fastlane_action: playground
    secrets: inherit
