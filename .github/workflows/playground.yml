name: Playground

on: [pull_request]
permissions: 
  checks: write

jobs:
  development:
    uses: wireapp/wire-ios-mono/.github/workflows/app_release.yml
    with:
      fastlane_action: playground
    secrets: inherit
