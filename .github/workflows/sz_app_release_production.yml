name: SZ Production
on: workflow_dispatch

permissions: 
  checks: write

jobs:
  sz_production:
    runs-on: ubuntu-24.04
    steps:
      - name: echo
        run: |
          echo "This workflow only exists on chore/add-sz-app for now"
