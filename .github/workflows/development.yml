name: Development

on: [pull_request]

permissions: 
  checks: write

jobs:
  development:
    uses: ./.github/workflows/_reusable_app_release.yml
    with:
      fastlane_action: development
    secrets: inherit
