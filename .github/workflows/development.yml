name: Development

on:
  workflow_dispatch:
  push:
    branches:
      - 'develop'

permissions: 
  checks: write

jobs:
  development:
    uses: wireapp/wire-ios-mono/.github/workflows/app_release.yml@main
    with:
      fastlane_action: development
    secrets: inherit
