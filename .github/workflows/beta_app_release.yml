name: Beta

on: [pull_request]

permissions: 
  checks: write

jobs:
  testflight_beta:
    uses: ./.github/workflows/_reusable_app_release.yml
    with:
      fastlane_action: testflight_beta
    secrets: inherit
